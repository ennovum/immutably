import {apply} from './apply';
import {set} from './set';
import {merge} from './merge';
import {clone} from './clone';

const immutably = {apply, set, merge, clone};

export default immutably;
export {immutably};
